# topo_profile_python — host-side helpers for `topo profile trace`.
#
# Currently exposes the cProfile NDJSON harness; additional helpers
# (py-spy spawn, sys.monitoring shim) land alongside as the profiler
# integration advances.
