// Purity violation: parallel stage function writes to a module global.

namespace app {
  public:
    void run();
    void compute();
    void render();

    fn run {
        stage<1> compute();
        stage<1> render();
    }
}
